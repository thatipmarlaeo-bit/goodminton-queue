# สถาปัตยกรรมระบบและบทบาทของผู้ใช้งาน (System Architecture & Actor Roles)
**โครงการ:** ระบบจัดการคิวสนามแบดมินตันแบบเรียลไทม์ (Real-time Badminton Court Management System)

---

## ส่วนที่ 1: โครงสร้างสถาปัตยกรรมระบบ (System Architecture)

ระบบนี้ถูกพัฒนาขึ้นด้วยรูปแบบ **Serverless & Real-time Architecture** โดยแบ่งแยกส่วนการทำงานหน้าบ้านและหลังบ้านอย่างชัดเจน เพื่อให้การแสดงผลสถานะคอร์ดและคิวเกิดขึ้นพร้อมกันในเสี้ยววินาที

*   **Frontend (ส่วนแสดงผล):** พัฒนาด้วย **Vue.js** ทำหน้าที่จัดการ UI/UX, รับค่าจากผู้ใช้งาน, คำนวณระยะทางพิกัด GPS บนเครื่องผู้ใช้ (Client-side Geofencing) และใช้ระบบเสียง/สั่น (Web APIs) สำหรับแจ้งเตือนเมื่อถึงคิว
*   **Backend & Database (ส่วนหลังบ้านและฐานข้อมูล):** จัดการโดย **Supabase (PostgreSQL)**
    *   **Database Schema:** ออกแบบตารางแยกย่อย (Normalization) ได้แก่ `user_profiles`, `queues`, `queue_members`, และ `courts`
    *   **SQL Views:** ใช้ View (`active_queues_view`) ในการทำ Data Aggregation มัดรวมข้อมูลผู้เล่นและสถานะคิวเข้าด้วยกันเพื่อลดภาระการดึงข้อมูลของหน้าบ้าน
    *   **Automation (ฝั่ง server):** ตรรกะการเรียบคิวลงสนามและจับเวลา ถูกย้ายมาอยู่ที่ฐานข้อมูลโดยตรง ไม่พึ่งพาหน้าจอใด ๆ ที่เปิดอยู่ ดูได้จาก `supabase/` folder ใน repo
*   **Realtime Engine:** ใช้ **Supabase Realtime Channels** ตรวจจับการเปลี่ยนแปลง (Postgres Changes) ของตารางคอร์ด คิว และสมาชิก หากมีการเปลี่ยนแปลง ระบบจะ Push ข้อมูลใหม่ไปยังหน้าจอผู้เล่นทุกคนที่เปิดแอปอยู่ทันที
*   **Identity Management:** ใช้ **Device ID** ประจำเครื่อง (บันทึกใน `localStorage`) เป็น Primary Key เพื่อระบุตัวตนแบบถาวร **ไม่มี Firebase/auth อีกต่อไป** (ถอดออกเพื่อตัดปัญหาข้อมูลแยกชุดระหว่าง device_id กับ uid)

---

## ส่วนที่ 2: บทบาทและตรรกะการทำงาน (Actors & Logic)

ระบบแบ่งผู้ที่มีส่วนร่วมในการทำงานออกเป็น 4 บทบาทหลัก (3 บทบาทมนุษย์ และ 1 บทบาทระบบ) ดังนี้:

### 1. ผู้เล่นทั่วไป (General Player)
**หน้าที่:** ผู้เข้าใช้งานทั่วไปที่ต้องการเล่นแบดมินตัน จัดการโปรไฟล์ของตนเอง และเข้าร่วมคิว
**ความสามารถและเงื่อนไข:**
*   **โปรไฟล์:** ลงทะเบียนและแก้ไขข้อมูล (ชื่อจริง, ชื่อเล่น, คณะ, อวาตาร์, ระดับฝีมือ) ข้อมูลจะผูกกับ device_id
*   **Geofencing Lock (GPS):** ระบบจะเช็กว่าผู้เล่นอยู่ในรัศมีสนาม (**<= 60 เมตร**) หรือไม่ หาก "อยู่" จะทำ Check-in (`INSIDE`) หาก "เดินออก" จะ Check-out (`OUTSIDE`) หากพิกัดอยู่นอกพื้นที่ ปุ่มกดสร้าง/แจมคิวจะถูกระงับการใช้งานทันที (ทำได้แค่ดูสถานะ) — **บังคับจริงทั้งหน้า player และ admin**
*   **แจมและออกคิว:** กดเข้าร่วมการ์ดคิวที่ยังไม่เต็ม หรือกดยกเลิก/ออกจากคิวที่ตนเองอยู่ได้
*   **One-Card Limit:** ผู้เล่น 1 คน มีชื่ออยู่ในคิวที่รอดำเนินการได้ **เพียง 1 คิวเท่านั้น** หากติดคิวอยู่ ปุ่มแจมคิวอื่นจะถูกบล็อก **และฐานข้อมูลกันซ้ำอีกระดับ** (trigger `enforce_one_active_queue` → error `23505`)
*   **แจ้งเตือนเมื่อถึงคิว:** เมื่อระบบเรียบคิวของตนเอง (คอร์ด → `CALLING`) หน้าจอจะสั่น + มีเสียง + แสดงแถบส้มกะพริบ

### 2. หัวหน้ากลุ่ม / ผู้สร้างคิว (Queue Creator)
**หน้าที่:** ผู้เล่นคนแรก (`players[0]`) ที่กดสร้าง "การ์ดคิว" จะได้รับสิทธิ์ในการดึงเพื่อนเข้าคิว
**ความสามารถและเงื่อนไข:**
*   **ดึงเพื่อนเข้าคิว (Invite Players):** มีปุ่มพิเศษเพื่อเปิดหน้าต่างดึงผู้เล่นอื่นที่อยู่บริเวณสนามเข้าคิวตนเองได้ทันที
*   **ระบบคัดกรองเพื่อน:** รายชื่อที่แสดงให้ดึงเข้าคิว ต้องเป็นผู้ที่ (1) Check-in อยู่ในสนาม (`INSIDE`), (2) ไม่ติดคิวอื่นอยู่เลย, และ (3) ไม่ใช่ตัวหัวหน้ากลุ่มเอง

### 3. ระบบอัตโนมัติ (Automated System) - *Core Engine*
**หน้าที่:** จัดการตรรกะการจัดลำดับและเรียกคิวลงสนาม (Matchmaking) แทนแรงงานคน เพื่อความแม่นยำและยุติธรรม
**รันบน Supabase โดยตรง** (`supabase/001_auto_call_trigger.sql` + `002_calling_timeout_cron.sql`)
**ตรรกะการรันคิว (Queue Processing Logic):**
1.  **Trigger Event:** เมื่อคอร์ดใดคอร์ดหนึ่งเปลี่ยนสถานะเป็นว่าง (`AVAILABLE`) trigger `trg_auto_call_next_queue` จะทำงานทันที (ใช้ `pg_advisory_xact_lock` กันสองคอร์ดชน)
2.  **คัดกรอง (Filtering):** มองหาเฉพาะการ์ดคิวที่ **"สมาชิกครบ 4 คน"** เท่านั้น (ทั้ง `WAITING` และ `SKIPPED` ต้องครบ 4)
3.  **จัดลำดับ (Priority Sorting):** เรียงลำดับคิวที่จะได้ลงสนามตามความสำคัญ:
    *   **Priority 1 (สิทธิ์ลัดคิว):** คิวที่มีสถานะ `SKIPPED` จะได้สิทธิ์ก่อน หากมีหลายกลุ่ม ใช้ `created_at` เป็นตัวตัดสิน
    *   **Priority 2 (ตามลำดับเวลา):** คิวที่ `WAITING` ปกติ เรียงแบบ FIFO ตาม `created_at`
4.  **เรียกคิว (Assignment & Alarm):** ระบบผูกคิวนั้นเข้ากับคอร์ด (queue → `ASSIGNED` + `assigned_court`), เปลี่ยนคอร์ดเป็น `CALLING`, เริ่มจับเวลา 3 นาที (ที่ `status_updated_at`) และส่ง Realtime ไปปลุกหน้าจอผู้เล่นทั้ง 4
5.  **หมดเวลา (Timeout):** หากกด "เริ่มเล่น" ไม่ทันใน 3 นาที ระบบจะคืนคอร์ดเป็น `AVAILABLE` (เรียบคิวถัดไปต่อ) และคิวเดิมกลับเป็น `ON_HOLD` — ตรวจผ่านฟรี 2 ทาง: (ก) **self-heal ใน trigger** ปลดเองทุกครั้งที่ trigger ทำงาน (ข) **watchdog ฝั่งหน้าแอป** ที่เปิดอยู่เรียก RPC `expire_calling_queues` เป็นจังหวะ (30-60 วิ) เพื่อให้ทำงานได้แม้คอร์ดไม่เปลี่ยนสถานะ

### 4. แอดมิน / เจ้าหน้าที่สนาม (Admin / Court Manager)
**หน้าที่:** ผู้ควบคุมสภาวะผิดปกติ (Exceptions) ที่ระบบอัตโนมัติไม่สามารถตัดสินใจเองได้ และคุมสถานะสนาม
**การเข้าถึง:** หน้า `/admin` ต้องกรอก **รหัส PIN** (เก็บไว้เป็น constant ใน AdminView.vue) ผลตรวจสอบผ่านเก็บเป็น session ใน `sessionStorage` เท่านั้น
**ความสามารถและเงื่อนไข:**
*   **ควบคุมเกม (Match Control):**
    *   กดยืนยันเริ่มเกม (`IN_PROGRESS`) เมื่อผู้เล่นทั้ง 4 เดินลงคอร์ดครบแล้ว
    *   กดจบเกมเพื่อเคลียร์คอร์ดให้เป็น `AVAILABLE` (ทริกเกอร์ให้ระบบอัตโนมัติทำงานต่อ) และคิวเก่าจะเปลี่ยนเป็น `FINISHED`
    *   สลับการแข่งขันระหว่างคอร์ด (swap court)
*   **จัดการสภาวะผิดปกติของคิว (Exceptions):**
    *   **พักคิว (`ON_HOLD`):** หากระบบเรียกแล้วคนมาไม่ครบ แอดมินกดพักคิวที่ถูกเรียกอยู่ (`holdCallingQueue`) เพื่อปล่อยคอร์ดให้คิวถัดไปขึ้นมาแทน หรือพักคิวที่รออยู่ (`holdQueue`) ออกจากแถว
    *   **ให้สิทธิ์เรียกก่อน (`SKIPPED`):** เมื่อคิวครบ 4 คนแล้ว แอดมินกดให้สิทธิ์ คิวจะถูกดันขึ้น Priority 1 (อ้างอิง `created_at` เดิม)
    *   **กลับสู่รอคิว (`WAITING`):** แอดมินเปลี่ยนจากพักคิวกลับเป็นรอปกติ คิวนั้นกลับเข้าตำแหน่งตาม `created_at` ทันที
    *   **ยกเลิกคิว (`CANCELLED`):** ยกเลิกคิวที่มีปัญหาหรือทุจริตทิ้ง
    *   **จัดการสมาชิก:** เพิ่มผู้เล่น (พิมพ์ค้นหา/เลือกจากรายชื่อในสนาม) หรือลบสมาชิกออกจากคิวได้
*   **ควบคุมสนาม:** สั่งเปิด/ปิดคอร์ดชั่วคราว (`CLOSED`) พร้อมระบุเหตุผล หากปิดทุกคอร์ด ระบบฝั่งผู้เล่นจะปิดกั้นการสร้างคิวและแจมคิวใหม่ทั้งหมดโดยอัตโนมัติ
*   **รายงาน:** สรุปยอดผู้เข้าใช้บริการประจำวัน (จำแนกตามบทบาท/คณะ) มุมภาพได้เป็น PDF

---

## ส่วนที่ 3: สถานะของคิวและคอร์ด (State Flow)

**สถานะคิว (`queues.status`)**
| สถานะ | ความหมาย |
|-------|---------|
| `WAITING` | รอรวมครบ 4 คน / รอคอร์ดว่าง |
| `SKIPPED` | ได้สิทธิ์เรียกก่อน (Priority 1) |
| `ON_HOLD` | ถูกพัก — ไม่อยู่ในแถวที่ระบบจะเรียก |
| `ASSIGNED` | ถูกจองคอร์ดแล้ว กำลังนับถอยหลัง (เรียกคิว) |
| `IN_PROGRESS` | กำลังเล่น |
| `FINISHED` / `CANCELLED` | จบ/ยกเลิก ไม่ถูกนับในคิวที่ active |

**สถานะคอร์ด (`courts.status`)**
| สถานะ | ความหมาย |
|-------|---------|
| `AVAILABLE` | ว่าง → trigger เรียบคิวถัดไปอัตโนมัติ |
| `CALLING` | กำลังเรียกคิว (มีเวลา 3 นาที แล้ว auto-revert) |
| `IN_PROGRESS` | กำลังแข่งขัน |
| `CLOSED` | ปิดชั่วคราว (มี `close_reason`) |

---

## ส่วนที่ 4: บันทึกการจัดวางใน Repo

*   `supabase/001_auto_call_trigger.sql` — trigger เลือกคิวถัดไปเมื่อคอร์ดว่าง + **self-heal** ปลดคอร์ด `CALLING` ค้าง (ฟรี ไม่พึ่ง pg_cron)
*   `supabase/002_calling_timeout_cron.sql` — (ตัวเลือก) cron ตรวจทุก 1 นาที เมื่อเปิดใช้งาน pg_cron ได้ (Add-on เสียเงิน — ระบบหลักใช้ self-heal + watchdog ฝั่งหน้าแอปแทน)
*   `supabase/003_one_card_guard.sql` — trigger กัน 1 คนอยู่หลายคิวที่ active (เพื่อล็อกตอน snapshot)
*   **ต้อง apply `001` (และ `003`) ใน Supabase Console** ถึงจะได้ระบบอัตโนมัติครบวงจร — `002` รันเฉพาะเมื่อต้องการตัวตรวจที่เข้มกว่านี้