-- =============================================================================
-- 005_merge_user_profiles_into_profiles.sql -- แก้เป็น "เอกสารเก็บทั้งสองตาราง"
-- -----------------------------------------------------------------------------
-- ⚠️ ข้อมูลจริงยืนยันแล้ว: โปรเจกต์ออกแบบให้ใช้ identity สองตารางร่วมกัน
--    -> **ห้าม drop ตารางใดตารางหนึ่ง**
--
--   * profiles        <- FK ของ queue_members/daily_checkins ชี้มา +
--                        live_courts_view อิงอยู่ (ตัวตนจริง / รายชื่อในสนาม)
--   * user_profiles   <- active_queues_view สร้างอยู่บนตารางนี้
--                        (รายชื่อ/avatar ในคิวที่ผู้เล่นเห็น)
--
-- การ "ลบ/รวม" ต้องเขียน view ทั้งสองใหม่ให้อิงตารางเดียว ซึ่งนิยามไม่อยู่ใน
-- repo -> ไม่ควรทำ เก็บทั้งสองไว้ (ข้อมูลครบ ใช้งานได้ตามปกติ)
--
-- หมายเหตุ: ถ้ารัน merge user_profiles->profiles ไปก่อนหน้า ("on conflict do
-- nothing" ไม่ทับของเดิม) ก็ไม่เป็นไร ไม่มีผลเสีย - ข้อมูลยังอยู่ครบทั้งสองที่
-- =============================================================================
-- ไม่มีคำสั่ง DML/Drop ในไฟล์นี้ (ตั้งใจ เหตุผลบน)
select 'keep both tables (profiles + user_profiles) — do NOT drop' as note;
