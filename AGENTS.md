# AGENTS.md

Vue 3 + Vite SPA for a badminton court queue system. UI text and source comments are in Thai (ระบบคิวสนามแบดมินตัน) — keep new user-facing strings and comments in Thai.

**STATUS (2026-09-20): ON HOLD by request — data model 001–009 applied, closed-app push is DEFERRED (code `010`+`011`+Edge Function `send-court-call` are ALL written and build-clean but NOT deployed: secrets/schedule not set, trigger `011` not applied).** Web push stays FREE/unlimited on OneSignal (the Sep 2026 caps hit mobile push MAU only — web push is unaffected), so when you resume: (1) apply `010` then `011` in Console SQL Editor, (2) set Edge Function secrets `ONESIGNAL_REST_API_KEY` + `ONESIGNAL_APP_ID`, (3) schedule `send-court-call` every ~1 min (or call from `QueueService.runCourtMaintenance`), (4) done. Until then, only the in-app realtime alarm works.

## Commands
- `npm run dev` — Vite dev server
- `npm run build` — production build (also the way to verify changes; no lint/typecheck/test tooling is configured)
- `npm run preview` — serve production build

No test framework exists. Do not invent lint/test commands.

## Backend architecture (important)
- **Supabase is the real backend.** All data access goes through `src/supabase.js` via `QueueService`/`UserService`. Tables/views (`queues`, `queue_members`, `courts`, `daily_checkins`, `profiles`, `active_queues_view`, `live_courts_view`) are defined server-side in Supabase — the schema is NOT in this repo, so column/name assumptions must match existing SQL queries in the services.
- **Canonical user table = `profiles` ONLY (do NOT drop/rename).** `queue_members.device_id` and `daily_checkins.device_id` are FKs to `profiles(device_id)`, and BOTH views (`active_queues_view`, `live_courts_view`) now join `profiles` only. The app reads/writes identity via `profiles` (`UserService.saveProfile`/`getProfile`), and `UserService.ensureIdentity()` guarantees a `profiles` row exists before any check-in so FK never blocks. No dual-write / mirror to any other table — `user_profiles` was dropped for real by `supabase/006_unify_identity.sql`; never reintroduce it.
- **Automation lives server-side in SQL, not in JS.** Files under `supabase/` must be applied manually in Supabase Console (SQL Editor → New query → Run; apply AFTER `001`; all idempotent with `drop ... if exists`/`create or replace` so re-running is safe — no `42710`): `001_auto_call_trigger.sql` (auto-assigns the next ready queue when a court turns `AVAILABLE`; also self-heals `CALLING` courts stuck > 3 min back to `AVAILABLE` + queue → `ON_HOLD` every time the trigger runs; advisory lock `83011`), `002_calling_timeout_cron.sql` (needs `pg_cron` paid Add-on — NOT installed; self-heal in `001` is the fallback), `003_one_card_guard.sql` (DB One-Card Limit via `23505`), `006_unify_identity.sql` (rebuilds `active_queues_view` onto `profiles` only then DROPS legacy `user_profiles`), `007_name_validation.sql` (DB CHECK `NOT VALID` — ชื่อจริงไทยล้วน ← ก-๙ / ชื่อเล่นไทย-อังกฤษ; `NOT VALID` = กันเฉพาะแถวใหม่), `008_auto_call_on_queue_full.sql` (NEW: when a queue reaches 4 members → immediately assign it to an `AVAILABLE` court, no need to wait for a court to free up; advisory lock `83012`; complements 001 which only fires on `court → AVAILABLE`), `009_auto_call_on_queue_resume.sql` (NEW: when a queue's status is updated back to `WAITING` — e.g. admin "กลับปกติ" from `SKIPPED`/`ON_HOLD` — immediately re-assign it to an `AVAILABLE` court instead of leaving it stuck `WAITING`; same lock `83012` as 008 so the two triggers serialize on the same queue and do not fight; added because 008 only fires on `queue_members` INSERT, not on `queues.status` UPDATE), `010_push_queue.sql` (NEW: `public.push_queue` work table — pending push jobs enqueued when a court flips to `CALLING`), `011_on_court_calling_enqueue_push.sql` (NEW: trigger `trg_enqueue_push_on_calling` on `courts` — when `status` becomes `CALLING` it collects the 4 `device_id`s of the queue that was called into `public.push_queue` so the Edge Function below can push to them; advisory lock `83013` so it serializes behind 008/009's `83012` instead of racing the same queue; guards on `old.status <> CALLING` so self-heal reassignments don't double-enqueue). Until applied, the system works but only with manual admin actions.
- **Deploy-order gotcha (006):** the app now reads/writes `profiles` ONLY. `006_unify_identity.sql` rebuilds `active_queues_view` (which joined the legacy `user_profiles`) to join `profiles` and then drops `user_profiles`. Because the view is what shows names in the queue list, you MUST run `006` in Supabase Console BEFORE deploying a new build — otherwise the queue list enqueue blank names until 006 runs. Do not deploy the new build before applying 006.
- **Match results / player stats (013):** `supabase/013_match_records.sql` creates `public.match_records` (queue_id unique, `player_device_ids` snapshot, `winner_device_ids` ≤ 2 — empty = เสมอ). Admin "จบเกม" now REQUIRES selecting winners (max 2, doubles) or pressing "เสมอ" before the court is freed (`AdminView.submitFinishResult` → `QueueService.recordMatchResult` → `finishMatch`). PlayerView shows plays/wins/draws on the edit-profile page from `QueueService.getPlayerMatchStats` (count via array `contains`). Apply `013` in Console SQL Editor (after 006) before the new build — until applied, "จบเกม" will fail with a missing-table error.
- Real-time updates use `postgres_changes` channels (`QueueService.subscribeToQueueChanges`, QueueService.js:28). Always `removeChannel` the old channel before subscribing again.
- Queue rules enforced in code + DB: max 4 members per queue; Postgres unique-violation code `23505` means "already in this queue / already in another queue".
- `/admin` is gated by a hardcoded PIN constant (`ADMIN_PIN` in AdminView.vue) + `sessionStorage` flag — not real auth. Do not make it real auth without being asked.

## Identity model
- No real auth. Identity is a `device_id` stored in localStorage key `badminton_local_device_id`; profile is cached under `badminton_user_profile`. Never rely on logged-in identity.

## Gotchas
- **Empty "เพิ่มผู้เล่น" invite modal: always lists `profiles` — Player = Admin (DONE, 2026-09-20).** `QueueService.getAvailablePlayersOnSite` (QueueService.js:241) uses `profiles` as the base **always**, no longer reads `system_settings.gps_filter_enabled` or `daily_checkins`:
  - Every row in `profiles` counts as "inside the venue" — no day-`INSIDE` daily-checkin gate, no GPS toggle. This is the Player = Admin parity per DP's "เปลี่ยนเกณฑ์ให้เหมือน Admin ตลอด" — do NOT re-add the daily_checkins-first path or a per-view toggle.
  - Still filters out anyone busy in a not-yet-finished queue (`fetchBusyDeviceIds({ skipErrorQueues: true })`), so a player already enqueued/serving isn't offered again in the modal.
  - The `system_settings` row `gps_filter_enabled` still exists and is still what the Admin "ปิดกรอง GIS" button writes, but it now only conveys the admin intent (used by the "gps filter" UI label), NOT the data source for this list. Use `profiles` as the base regardless.
- Check-in enforces GPS proximity (~60 m) to the venue for all users (both PlayerView and AdminView). There is a `DEV_BYPASS_GPS = false` flag in PlayerView.vue — do not enable it except for local demos. PlayerView also has a UI toggle? No — instead, AdminView has an admin-only "ปิดกรอง GIS" button (writes `system_settings` row `gps_filter_enabled`; both views read it + subscribe via realtime; a PlayerView `DEV_BYPASS_GPS = false` flag remains for local demos only). When filtering is off, every page treats all users as inside the venue — for testing, admin-powered only.
- Big flat SFCs: PlayerView.vue (~1460 lines) and AdminView.vue (~2005 lines); add to them rather than restructuring until asked.
- **Header avatar = shortcut to edit profile (PlayerView).** The avatar in the player header (`PlayerView.vue`) is clickable → on tap it opens the "แก้ไขข้อมูลผู้ใช้งาน" form via `resetPlayer` (same as the "แก้ไขโปรไฟล์" button, which is hidden once the header collapses on scroll). Do not remove the click handler — it's the only entry point to edit-profile while scrolled down.
- Dark mode: Tailwind `darkMode: 'class'`, controlled by `src/composables/useTheme.js`.
- API keys are hardcoded in `src/supabase.js` (public anon/JS key — this is intentional for the SPA).
- OneSignal web-push SDK is initialized in `index.html` (init + `OneSignal.login(devId)` only — `devId` = `device_id`, maps to OneSignal `external_id`). In-app realtime alarm when a court flips to `CALLING` (PlayerView watch on `courts`). Closed-app web push is ALSO wired up (web push is free/unlimited on OneSignal): court → `CALLING` enqueues the queue's 4 `device_id`s into `public.push_queue` via trigger `011_on_court_calling_enqueue_push.sql` (advisory lock `83013` so it serializes behind 008/009's `83012`), and Edge Function `send-court-call` (`supabase/functions/send-court-call/`) polls `push_queue` where `sent_at is null` every ~1 min → OneSignal REST `include_aliases.external_id` → marks `sent_at`. Secrets must be set in Supabase Console (project settings → Edge Functions secrets): `ONESIGNAL_REST_API_KEY` + `ONESIGNAL_APP_ID` (and schedule the function via Console → Edge Functions → send-court-call → Schedule, or call it from `QueueService.runCourtMaintenance` — it's idempotent, `sent_at` guards against double-send). Until secrets+schedule are set, only the in-app alarm works.
- First-load redirect is SPA-only: Vercel rewrites all routes to `index.html` (vercel.json). `dist/` is gitignored.

## Config quirks
- **Admin "จัดการคอร์ด" header aligned with Player "สถานะสนามปัจจุบัน" (FIXED, 2026-09-20).** The admin courts section heading (`AdminView.vue`) was `text-xs` on a `mb-3` row — smaller/inconsistent vs. PlayerView's `ml-1 text-base leading-none` heading on a `mb-2 px-1` row. Fix: same classes as the player heading (`text-base`, `ml-1`, `leading-none`, row `flex items-center justify-between mb-2 px-1`); admin keeps `text-slate-400` since its page is always dark.
- **Admin page double padding (FIXED, 2026-09-20).** The authenticated area root (`AdminView.vue`) used to carry `p-4 sm:p-6` on the outer `min-h-screen` div AND `p-3 md:p-6` on the inner `max-w-6xl mx-auto` container — stacking ~28px (mobile) / ~48px (md+) gutters per side and squeezing admin content inward vs. PlayerView's single `p-3 md:p-6`. Fix: removed `p-4 sm:p-6` from the outer div; the single note in the PlayerView container rules the outside; do not re-add padding to the outer wrapper (modals are `fixed inset-0` and unaffected).
- Vite `manualChunks` splits Vue into a separate vendor chunk; `chunkSizeWarningLimit` raised to 1000 kB (vite.config.js).
- `.env.local` contains a Vercel OIDC token — it is gitignored; never commit it.